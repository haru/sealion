# Postgres service credentials — used by both the db service and app's DATABASE_URL
# (DATABASE_URL is auto-generated in docker-compose.yml from these values)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=sealion_dev

# Auth.js — generate with: docker compose run --rm --no-deps sealion generate-keys
AUTH_SECRET=""

# Credential encryption key — 64 hex chars
# Generate with: docker compose run --rm --no-deps sealion generate-keys
CREDENTIALS_ENCRYPTION_KEY=""

# App URL — set to the public URL of the app (used by Auth.js for redirect resolution)
# AUTH_URL=http://your-server-host:3000

# Optional: Host-side port mapping (container always listens on 3000)
# HOST_PORT=3000

# Optional: DB healthcheck variables (used by docker/entrypoint.sh)
# DB_PORT=5432
# DB_USER=postgres
# DB_MAX_RETRIES=30
# DB_RETRY_INTERVAL=2
