# Required because next-auth@5 beta declares peerOptional nodemailer@^7,
# but we need nodemailer@^8 to resolve security vulnerabilities (GHSA-c7w3-x93f-qmm8, GHSA-vvjj-xcjg-gr5g).
# Remove this once next-auth supports nodemailer@^8.
legacy-peer-deps=true
